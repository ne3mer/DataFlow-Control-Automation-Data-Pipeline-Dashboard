# 🕷️ راهنمای تست اسکراپر - How to Test Scraper

## 📋 مراحل تست اسکراپر

### 1️⃣ ساخت Job اسکراپر از طریق سایت

1. **وارد سایت شوید** (اگر لاگین نیستید، ابتدا لاگین کنید)
2. **به صفحه Jobs بروید** (`/jobs`)
3. **روی دکمه "Create Job" کلیک کنید**
4. **فرم را پر کنید:**
   - **Name:** یک نام برای job (مثلاً: "Scrape Example.com")
   - **Type:** از dropdown انتخاب کنید `Scraper`
   - **Target URL:** آدرس سایت مورد نظر را وارد کنید
     - مثال: `https://example.com`
     - مثال: `https://news.ycombinator.com`
     - مثال: `https://github.com`
   - **Schedule (Cron):** خالی بگذارید (برای تست دستی) یا یک cron expression وارد کنید
5. **روی "Create" کلیک کنید**

### 2️⃣ اجرای Job

1. **در لیست Jobs**، job جدید شما را پیدا کنید
2. **روی دکمه "Run" کلیک کنید**
3. **صبر کنید** تا status به `running` و سپس `completed` تغییر کند (حدود 5-10 ثانیه)

### 3️⃣ مشاهده نتایج

1. **روی دکمه "View" کلیک کنید** (کنار job)
2. **در صفحه Job Detail** شما می‌توانید ببینید:
   - **Configuration:** تنظیمات job
   - **Live Logs:** لاگ‌های زنده (اگر job در حال اجرا باشد)
   - **Run History:** تاریخچه اجراها
   - **📊 Scraped Data:** داده‌های استخراج شده از آخرین اجرا شامل:
     - عنوان صفحه (Title)
     - لینک صفحه
     - توضیحات متا (Meta Description)
     - پاراگراف اول
     - تعداد لینک‌ها و تصاویر
     - نمونه لینک‌ها (قابل کلیک)
     - نمونه تصاویر (نمایش تصویری)

---

## 🎯 مثال‌های سایت برای تست

### سایت‌های ساده (برای تست اولیه):

- `https://example.com` - سایت ساده و استاندارد
- `https://httpbin.org/html` - تست HTML

### سایت‌های واقعی (داده‌های بیشتر):

- `https://news.ycombinator.com` - لینک‌های زیاد
- `https://github.com` - محتوای غنی
- `https://www.wikipedia.org` - متن و لینک‌های زیاد

---

## 🔧 تنظیمات پیشرفته

### اجرای خودکار با Cron:

اگر می‌خواهید job به صورت خودکار اجرا شود، در فیلد **Schedule** یک cron expression وارد کنید:

```
* * * * *     # هر دقیقه
0 * * * *     # هر ساعت
0 0 * * *     # هر روز در نیمه شب
0 9 * * 1     # هر دوشنبه ساعت 9 صبح
```

---

## 📊 داده‌هایی که استخراج می‌شوند:

✅ **Title** - عنوان صفحه  
✅ **Meta Description** - توضیحات متا  
✅ **Links** - لینک‌های صفحه (تا 20 لینک اول)  
✅ **Images** - تصاویر صفحه (تا 10 تصویر اول)  
✅ **First Paragraph** - پاراگراف اول متن  
✅ **Content Length** - حجم محتوا  
✅ **HTTP Status** - کد وضعیت HTTP

---

## ⚠️ نکات مهم:

1. **برخی سایت‌ها ممکن است اسکراپ را بلاک کنند** - این طبیعی است
2. **اگر job با خطا مواجه شد**، در Run History می‌توانید جزئیات را ببینید
3. **برای سایت‌های بزرگ**، فقط بخشی از داده‌ها نمایش داده می‌شود (برای عملکرد بهتر)
4. **لاگ‌های زنده** فقط زمانی کار می‌کنند که job در حال اجرا باشد

---

## 🐛 عیب‌یابی (Troubleshooting):

### اگر job فیل شد:

- بررسی کنید که URL صحیح است
- بررسی کنید که سایت در دسترس است
- در Run History، لاگ‌ها را بررسی کنید

### اگر داده‌ها نمایش داده نمی‌شوند:

- مطمئن شوید که job با موفقیت اجرا شده (`status: completed`)
- صفحه را refresh کنید
- بررسی کنید که job از نوع `scraper` است

---

## 🚀 مثال کامل:

```
1. Create Job:
   Name: "Scrape Hacker News"
   Type: Scraper
   URL: https://news.ycombinator.com
   Schedule: (خالی)

2. Run Job → صبر کنید تا completed شود

3. View Job → ببینید:
   - Title: "Hacker News"
   - Links: 30+ links
   - Images: چند تصویر
   - Sample links قابل کلیک
```

---

**موفق باشید! 🎉**
